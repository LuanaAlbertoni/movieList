import { createStyles } from "@material-ui/core/styles";

const classes = () =>
  createStyles({
    rateStar: {
      cursor: "pointer",
    },
    root: {
      padding: "16px",
      border: "1px solid #ababab",
      borderRadius: "16px",
    },
  });

export default classes;
